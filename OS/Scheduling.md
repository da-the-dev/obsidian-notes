In multiprogrammed computers, programs fight for the same [[CPU]].
Scheduler must be effective, so not to hog the system.
[[Process]] switching is **expensive**.

# When to schedule
- When a new process is created